% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/options.R
\name{rclade_options}
\alias{rclade_options}
\title{Construct a validated options list for \code{plot_timetree()}}
\usage{
rclade_options(
  rank = "none",
  layout = "rectangular",
  color_palette = "viridis",
  taxonomy_format = "auto",
  add_timescale = TRUE,
  timescale_mode = "radial",
  unit = NULL,
  legend_position = "bottom",
  line_width = 1,
  show_tip_labels = FALSE,
  width = 14,
  height = 10,
  ...
)
}
\arguments{
\item{rank}{Character. Collapsing rank (e.g. "phylum", "class", "none").}

\item{layout}{Character. "rectangular" or "circular".}

\item{color_palette}{Character. Palette name (e.g. "viridis", "plasma", "Set1").}

\item{taxonomy_format}{Character. One of "auto", "GTDB", "Silva", "NCBI",
"embedded", "custom_regex".}

\item{add_timescale}{Logical. Whether to add a geological timescale.}

\item{timescale_mode}{Character. "radial", "linear", "none".}

\item{unit}{Character or NULL. "Ma", "Ga", or NULL. Required when
add_timescale = TRUE (the pipeline aborts for NULL); NULL leaves native
units untouched and is only valid with add_timescale = FALSE.}

\item{legend_position}{Character. Legend placement (e.g. "bottom", "right", "none").}

\item{line_width}{Numeric. Branch line width.}

\item{show_tip_labels}{Logical. Whether to display tip labels.}

\item{width}{Numeric. Output width in inches.}

\item{height}{Numeric. Output height in inches.}

\item{...}{Additional named parameters to include in the options list.
These are passed through without validation.}
}
\value{
A named list of class \code{"rclade_options"} suitable for the
\code{opts} argument of \code{plot_timetree()}.
}
\description{
\code{rclade_options()} returns a named list of commonly used rendering
parameters that can be passed to \code{plot_timetree(opts = ...)} as a
single object.  Explicit arguments supplied directly to
\code{plot_timetree()} always take precedence over values in \code{opts}.
}
\details{
This constructor is the first step toward parameter-surface convergence:
it provides a single validated source of truth for parameter defaults,
reducing the risk of drift between the \code{plot_timetree()} signature,
internal forwarding lists, and documentation.
}
\examples{
# Create a reusable options object
opts <- rclade_options(rank = "phylum", layout = "circular",
                       color_palette = "plasma", add_timescale = TRUE)

}
