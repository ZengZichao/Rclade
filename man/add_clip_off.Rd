% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/params-helpers.R
\name{add_clip_off}
\alias{add_clip_off}
\title{Apply coordinate-system clip="off" (the only clip application point)}
\usage{
add_clip_off(p, layout = "rectangular")
}
\arguments{
\item{p}{A ggplot object.}

\item{layout}{"rectangular" or "circular".}
}
\value{
The ggplot object with the appropriate clip-off coordinate added.
}
\description{
Collapsed-triangle vertices (especially the MRCA apex) often extend beyond
the tip-based y-axis range, so the plot panel must NOT clip them.  This
helper is the single, sanctioned place to add \code{clip = "off"}:
\itemize{
\item rectangular -> \code{coord_cartesian(clip = "off")}
\item circular/fan -> \code{coord_polar(theta = "y", clip = "off")}
}
}
\details{
A \code{utils::packageVersion("ggplot2")} guard protects against ggplot2
< 3.5.0 (which does not support \code{clip = "off"}): in that case we
\code{log_warning} and skip clip rather than silently mis-rendering.
}
\keyword{internal}
