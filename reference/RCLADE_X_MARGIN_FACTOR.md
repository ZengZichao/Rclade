# Default margin factor for x-axis range extension

Extends the x-axis range by 5% beyond the tree depth to prevent tip
labels from being clipped at the plot boundary. Source: ICS
chronostratigraphic chart convention for visual padding.

## Usage

``` r
RCLADE_X_MARGIN_FACTOR
```
