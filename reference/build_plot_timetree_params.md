# Build plot_timetree parameter list from CLI options

Build plot_timetree parameter list from CLI options

## Usage

``` r
build_plot_timetree_params(
  opt,
  color_palette,
  color_mapping = NULL,
  taxonomy_levels = NULL
)
```
