# Release the advisory log file lock acquired by `.acquire_log_lock()`

Release the advisory log file lock acquired by
[`.acquire_log_lock()`](https://zengzichao.github.io/Rclade/reference/dot-acquire_log_lock.md)

## Usage

``` r
.release_log_lock()
```
